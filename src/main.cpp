#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // Skeleton: read all input lines and ignore (no output).
    // Will be implemented in subsequent commits.
    string line;
    while (getline(cin, line)) {
        // do nothing for now
        (void)line;
    }
    return 0;
}
